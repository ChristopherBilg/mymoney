import React from 'react';
import SidebarMenu from './SidebarMenu';

export default {
  title: 'SidebarMenu',
};

export const noProps = () => <SidebarMenu />;
export const isOpen = () => <SidebarMenu isOpen />;
