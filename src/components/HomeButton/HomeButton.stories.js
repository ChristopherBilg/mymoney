import React from 'react';
import HomeButton from './HomeButton';

export default {
  title: 'HomeButton',
};

export const noProps = () => <HomeButton />;
export const isText = () => <HomeButton isText />;
